# Reborn method illustration

Generated with the built-in image generation tool. Schematic illustration, not experimental evidence. The two panels represent jointly trained objectives, not separate pretraining phases. Harmony weighting is omitted to focus on the two core components.

## Generation prompt

Selected asset: `reborn_two_stage_v2.png`. Follow-up edit prompt: Preserve the upper pipeline and panels; change the bottom control ribbon so sampled z feeds Actor and Critic in parallel, only Actor feeds the action, and the action feeds RSSM imagination. Generated illustrations are conceptual; arrows and scientific notation should be redrawn/verified before camera-ready use.

Use case: scientific-educational. Create a publication-quality main method illustration for a NeurIPS/ICLR machine-learning paper. Wide landscape, white background, sharp vector-like raster, sophisticated minimal typography, restrained teal/blue/ochre palette, meticulous spacing, no 3D, no shadows, no decorative brain icons. Prioritize visual explanation over text. Title small: "CoRe-WM · Reborn". Two large balanced panels labelled "(a) Spatial refinement" and "(b) Multi-timescale prediction". These are jointly trained auxiliary objectives, NOT sequential pretraining phases.

A shared small input pipeline across upper left: an original tiny Atari-like road-game observation with car and lane markers -> box "RSSM" -> h_t -> box "Affine + noise" -> segmented code with FIVE color-coded blocks. Small equation z = μ(h) + ε, ε ~ N(0,I). Present five nested prefixes by stacked colored bars of increasing length, label only "1", "2", "3", "4", "5". Consistent block colors throughout.

Panel a: show these nested prefix bars feeding five tiny decoder nodes, each receives the entire corresponding prefix (not isolated new block). Outputs are fixed orthogonal Haar bands: first a 4×4 coarse road-image mosaic; next four visual signed detail bands with progressively finer edges. Addition symbols between bands and five progressively refined reconstructions from coarse 4×4 appearance through 8,16,32 to crisp64. Same scene every time, no change of car position. Tiny labels "4", "8", "16", "32", "64" below the output thumbnails. Show fixed target bands from the observation as a small aligned reference strip with dashed supervision connectors. Label "Fixed Haar bands". Small lock glyph next to coarse output and short label "Coarse content preserved". A tiny descending five-bar rate-cost glyph beneath code, label "Cumulative rate", with equation R≤ℓ = ½‖μ≤ℓ‖². It denotes higher per-unit rate cost for earlier blocks, NOT empirical information content. Do not depict fabricated experimental charts or guarantee noncollapse.

Panel b: repeat FIVE nested prefix bars in five rows, short prefix on top and full on bottom. Each plus current action a_t icon feeds one predictor; output branches to a stylized future-observation aggregate thumbnail and reward/action-value symbol. Title over outputs "Future observation + action-value". Aligned temporal kernel sketches in each row: top long smoothly decaying kernel, next progressively shorter, bottom single impulse. Labels "16", "8", "4", "2", "1" and one shared label "Effective horizon". These sketches are mathematical discount weights, not performance graphs. Show ghosted future road frames under top long kernel to suggest cumulative future outcomes, not one endpoint prediction. Compact bottom target pathway real transition -> next image + reward -> dashed arrow through "EMA · stop-grad" to predictors; label "One-step TD targets". No latent slowness or persistence penalty, no future-action sequence input.

A thin separate bottom ribbon, visually subordinate: "Dreamer control" with RSSM imagination -> the SAME sampled segmented z -> Actor / Critic -> small joystick. Clearly do not route actor through decoded images or successor output. Small footer equation only "L = L_WM + λ₁ L_spatial + λ₂ L_outcome". Do not add Harmony boxes or claims of improved scores. Overall reading order clear, generous white space, precise arrows with no crossings, text minimal and legible. All illustrative game frames are schematic not experimental results.
