# CoRe-WM two-stage illustration

Generated with the built-in image generation tool. Based on run_warmup_arm.sh and dreamerv3/htp.py. This depicts scaffold warm-up followed by auxiliary temporal refinement, not two sequential auxiliary losses. Experimental persistence and causal-average constraints are excluded. Raster concept figure: inspect typography and auxiliary wiring before camera-ready use; the inactive warm-up branches remain visually anchored near h rather than z despite the correction request. All bars and horizons are schematic.

## Initial prompt

Use case: scientific-educational. Create a publication-quality scientific method illustration for CoRe-WM, suitable for a NeurIPS/ICLR paper. White background, wide landscape 3:2 or 16:9 high-resolution composition, exquisitely clean flat vector-like geometry, meticulous alignment, generous whitespace, precise thin arrows, beautiful readable sans-serif typography, muted teal/blue/amber/violet palette with charcoal text. Prioritize visual explanation, minimal text, no paragraphs, no decorative AI brains, no gradients or 3D or fake empirical plots. Title small: "CoRe-WM". Two clearly numbered panels, left about one third, right two thirds. Exact panel headings "1  Scaffold warm-up" and "2  Temporal refinement".

SCIENTIFIC CONTENT:
Stage 1: compact observation-frame stack icon feeds rounded "RSSM" module -> small h_t feature bar -> "Sψ" projection -> z_t segmented bar -> "Actor / Critic" module. Small auxiliary branches greyed out with simple off-toggle icon and label "Auxiliary OFF". Projection already exists and is trained in this phase; do NOT depict plain Dreamer without projection, do NOT show a frozen backbone. Transition arrow between panels labeled "Same architecture".

Stage 2 top central pipeline repeats observations -> RSSM -> h_t -> Sψ -> segmented z_t -> Actor / Critic. Full z_t feeds policy, NOT only compact prefix. Use five adjoining colored blocks for ordered z, each consistently colored wherever reused; cumulative prefix lengths increase as 128,256,512,1024,2048. Widths can be schematic; label endpoints only if needed. Below this pipeline, two clean visual subpanels, labeled "Progressive reconstruction" and "Multi-stride dynamics".
Reconstruction subpanel: three representative rows with ellipsis to indicate five levels, row1 first teal block -> G1 -> reconstructed h, row2 next blue block -> G2 -> plus node with a dashed stop-gradient connection from preceding cumulative reconstruction -> refined h, row3 ellipsis -> larger cumulative reconstruction. Use feature bars rather than photographs for reconstruction outputs; output h is RSSM features not pixels. Shared target small feature bar "sg(h_t)" with thin dashed loss connectors. Label dashed carry once "sg". Keep routing neat, no crossed arrows.
Dynamics subpanel should be the visual focal point: five aligned time lanes on a common horizontal time axis t to t+16. At left each lane has a cumulative prefix: 1 teal block; teal+blue; teal+blue+amber; teal+blue+amber+violet; all five blocks. From each prefix through small predictor box F1,F2,F3,F4,F5 draw an arrow to a same-width matching target prefix at its assigned future time. Endpoints: smallest prefix furthest right at t+16, then t+8, t+4, t+2, largest prefix at t+1. Layout staggered endpoints must clearly communicate small prefix LONG horizon, large prefix SHORT horizon. Tiny action-token sequence above prediction arrows labeled "actions". Target prefix bars have dashed outlines to signify stop-gradient EMA targets; tiny shared label "EMA targets · sg". Not five identical time lengths, not independent noncumulative blocks. Small legend underneath: "Compact → long horizon" and "Full → short horizon". These are assigned prediction horizons, NOT proven semantic invariance.
Footer single understated text: "Auxiliary objectives; control uses full z". Do not suggest auxiliary predictors unroll policy imagination. Do not include new causal averaging / persistence refinements, numeric performance claims, guaranteed stability, frozen backbone, or imagined empirical trends. This figure describes the established two-stage training design only.
Overall editorial design: strong information hierarchy, restrained fine outlines, no excessive enclosing boxes, use pictograms and color-consistent latent bars rather than verbose text. Render all text sharply and correctly.

## Refinement 1

Edit this scientific figure, preserving its overall two-panel layout, typography, colors, and style. Correct the dataflow and remove misleading details. In progressive reconstruction ONLY: label inputs "new block", G1 receives only teal, G2 receives only blue, G5 receives only dark blue; no cumulative prefix bars at these decoder inputs. Keep residual additions and sg links, h target. In multi-stride dynamics ONLY: remove the common bottom time axis because this schematic is not to scale; retain row labels t+16,t+8,t+4,t+2,t+1. Arrow spans must strictly decrease from top to bottom, by moving predictor outputs and target bars appropriately: row1 longest, row2 next, row3 medium, row4 short, row5 shortest; arrange target bars as descending staircase toward LEFT from top to bottom. Targets contain exactly 1,2,3,4,5 colored blocks matching the corresponding input cumulative prefixes: fourth target must have FOUR blocks not five. Label lower area "Prediction horizon (schematic)" instead of any time axis. Retain "Compact → long horizon" and "Full → short horizon" without decorative colored wedge. In warm-up panel remove the invented "Other auxiliary" branch entirely, leaving only reconstruction and dynamics OFF. Change observation notation below BOTH frame stacks from o_{t:t+K} to o_{≤t}, to avoid suggesting future observations enter h_t. Grey OFF branches should originate from z_t rather than h_t. Keep everything else unchanged, no extra text.

## Refinement 2

Correct this figure's scientific labels without changing overall layout. Panel B title stays Multi-stride dynamics. Change its "new block" label to "prefix". In panel B replace ALL colored output bars to the RIGHT of F1..F5 with simple dashed rounded rectangles containing text, one rectangle per row. No colored tiles inside these output boxes. EXACT output labels top to bottom: "128-d target", "256-d target", "512-d target", "1024-d target", "2048-d target". Keep corresponding t+16,t+8,t+4,t+2,t+1 labels. Above F boxes add a small label "action-conditioned". Keep input colored cumulative prefixes as they are. In panel A remove the numbers 128,256,2048 below the single block icons because they incorrectly describe cumulative dimensions as block dimensions. Remove the floating "sg" and plus node on FIRST reconstruction row only; G1 connects directly to reconstructed h1. Keep dashed sg carry from h1 into row2 plus, keep row5 plus. On left stage1 reroute grey dashed auxiliary branch to originate beneath z bar instead of h bar. All other elements unchanged.

